var Header = React.createClass({

  render: function() {
    return (
      <div>
          <p>Mon header react</p>
      </div>
    );
  }
});


ReactDOM.render(<Header />, document.getElementById('header'));
